import React from 'react';
import SearchBar from './components/SearchBar/SearchBar';

const Courses = (props) => {
	const courses = [];
	return (
		<div>
			<SearchBar />
			courses
		</div>
	);
};

export default Courses;
